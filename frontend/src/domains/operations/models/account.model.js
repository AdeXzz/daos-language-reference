export class Account {
    constructor(id, accountNumber, accountType, balance, createdAt) {
        this.id = id
        this.accountNumber = accountNumber
        this.accountType = accountType
        this.balance = balance
        this.createdAt = createdAt
    }
}